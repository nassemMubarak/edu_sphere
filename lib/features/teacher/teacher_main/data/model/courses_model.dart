class CoursesModel {
  String title;
  String description;
  bool isPending;
  bool isRejected;
  CoursesModel({
    required this.title,
    required this.description,
     required this.isPending,
     required this.isRejected
  });
}
